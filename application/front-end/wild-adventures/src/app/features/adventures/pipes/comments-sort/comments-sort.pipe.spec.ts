import { CommentsSortPipe } from './comments-sort.pipe';

describe('CommentsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
