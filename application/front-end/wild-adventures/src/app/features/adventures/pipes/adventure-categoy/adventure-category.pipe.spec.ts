import { AdventureCategoryPipe } from './adventure-category.pipe';

describe('AdventureCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new AdventureCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
